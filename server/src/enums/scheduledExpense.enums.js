const frequency = {
    monthly: 'MONTHLY',
};

const sortFields = {
    nextRunAt: 'nextRunAt',
    amount: 'amount',
    createdAt: 'createdAt',
};

export default {
    frequency,
    sortFields,
};
